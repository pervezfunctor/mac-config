# Minimal MacOS Config

## Bootstrap

Run the bootstrap script. This installs recommended shell tools, vscode, kitty and ai tools. You might be asked to install xcode command line tools.

```sh
git clone https://github.com/pervezfunctor/mac-config.git ~/.mac-config
sh ~/.mac-config/mac-setup
```

Restart your terminal.
